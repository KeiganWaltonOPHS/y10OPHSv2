cars = []
loop = True

while loop == True:
  newcar = (input("Enter a car company and press x to exit"))
  if newcar == "x":
    print("exiting....")
    loop = False
  else: 
    cars.append(newcar)
    
print ("in the list =", cars
