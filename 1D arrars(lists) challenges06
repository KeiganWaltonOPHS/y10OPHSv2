nums = []
count = 0

while True:
  n = int(input("Enter a number"))
  nums.append(n)
  count += 1 
  
  if count ==3:.
  
    delete = input("Do you want to keep the last number?").lower()
    
    if delete == "n":
    
    
    
    
    still need to edit
